[project]
name = "Valobot"
version = "0.1.0"
description = "This project is a bot for managing Valorant custom games on Discord."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "discord-py>=2.7.1",
    "python-dotenv>=1.0.0",
]
